package MainUI;

import javax.swing.*;
import java.awt.*;

public class QuizPanel extends JPanel {
    public QuizPanel() {
        setLayout(new BorderLayout());
//        add(add,BorderLayout.CENTER);
    }
}
